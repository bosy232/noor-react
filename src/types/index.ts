export interface Doctor {
  name: string;
  specialty: string;
  image?: string;
}

export interface Service {
  title: string;
  description: string;
  icon: string;
}

export interface Department {
  name: string;
  description: string;
  image: string;
}
